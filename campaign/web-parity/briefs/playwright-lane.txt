SEALED BRIEF — stand up a real-browser UI test harness for the mux web client.

Context: a vanilla-JS web terminal client (xterm.js) served by a Node/Express relay. Its tests today assert against the HTML file AS TEXT (regex over source). That is why a change which passed every test still froze the live terminal: source assertions cannot see a browser-to-server feedback loop, layout, or whether a control is reachable. This lane adds the missing layer. No repo browsing beyond the paths named here.

WHAT EXISTS
- Relay app: relay/server.js. Test harness: relay/tests/harness.js exports { RelayHarness, freePort, sleep, waitFor, leaseCommands, ackLeased, waitForWsText, FakeHost, REPO, HOST_CAPS }.
  `new RelayHarness()` then `await h.start()` boots a REAL relay on a free port (h.port), with
  MUX_TEST_MODE=1 and a temp state dir; `await h.stop()` tears it down. `FakeHost` plays the PC-side
  muxd over the host websocket.
- Client pages served by that relay: `/` (index.html, the terminal), `/chats.html`, `/projects.html`.
- Test runner: `node --test`, CommonJS, `npm test` runs `node --test "tests/**/*.test.js"` from relay/.
- Playwright is NOT installed. Install it as a devDependency of relay/ (`@playwright/test` or the
  `playwright` library — your choice, justify it) plus the chromium browser binary only.

FROZEN CONTRACT
- One new test file: relay/tests/ui-browser.test.js, using node:test (NOT the playwright test
  runner) so it runs under the existing `npm test`. Use the playwright LIBRARY to drive chromium
  headless.
- It must SKIP CLEANLY (not fail) when the chromium binary is unavailable, printing how to install
  it. CI and fresh clones must not go red because a browser is missing.
- Boot a real RelayHarness, attach a FakeHost so a session exists, then drive the real page.
- Cover these four, each of which a source assertion provably cannot:
  1. VIEWPORT/RESIZE STORM REGRESSION: emulate a mobile viewport, then scroll the page, and assert
     the client does NOT emit a burst of viewport/size reports to the server. (The frozen-terminal
     bug was: page scroll -> re-measure -> PTY resize, repeatedly.) Assert a bounded number of
     size reports over a scroll sequence.
  2. KEYBAR TAP: tapping a #keybar button fires its action on the FIRST tap and does not move focus
     away from the terminal's hidden textarea (document.activeElement must still be the terminal
     input after the tap).
  3. HORIZONTAL PAN REACHABILITY: with a session grid wider than the viewport, the pan affordance
     exists and actually moves the view (scrollLeft changes, or the transform does) — the thing the
     DOM-level test explicitly could not prove.
  4. CHATS PAGE OFFLINE STATE: load /chats.html with the discovery API unreachable and assert the
     explicit "PC archive unavailable" state renders rather than an infinite spinner.
- Deterministic: no arbitrary sleeps as synchronisation; use waitFor/expect-style polling with a
  bounded timeout. Every assertion message must name the USER-VISIBLE consequence.
- Do not modify relay/server.js, relay/public/**, or any existing test file. If a page genuinely
  cannot be tested without a change to it, say so in AMBIGUITIES rather than changing it.
- Update relay/package.json ONLY to add the devDependency (and a `test:ui` script if useful).

DELIVERABLE: Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\relay\tests\ui-browser.test.js (plus the relay/package.json devDependency line)

Run this yourself before reporting, from Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\relay:
VERIFY: node --test tests/ui-browser.test.js

Report the pass/fail table AND paste the exact install commands you ran for the browser binary.
If a check cannot be made deterministic, mark it skipped with a written reason instead of shipping
a flaky test. Finish with AMBIGUITIES. Turn budget ~30 minutes.
