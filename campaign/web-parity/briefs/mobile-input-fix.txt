SEALED BRIEF — mobile input reliability: keyboard-aware viewport + keybar focus theft.

Context: a single-file web terminal client (xterm.js in a plain HTML page). Two REPRODUCED user-facing defects on phones. You fix both in ONE file. Everything needed is inlined below; no repo browsing is required.

FILE YOU EDIT (the only one): Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\relay\public\index.html

=== DEFECT 1: the soft keyboard covers the input; the page never shrinks ===
Current code (around line 1544):

function applyViewport(){
  _lastViewportResizeAt=Date.now();
  const vv=window.visualViewport;
  if(vv && vv.height>0) $('#app').style.height = vv.height+'px';
  scheduleViewportFit(0);
}
if (window.visualViewport){ visualViewport.addEventListener('resize', ()=>scheduleViewportFit(isMobile()?140:40)); }

`applyViewport()` is THE function that shrinks the app to the keyboard-reduced visual viewport, but the visualViewport 'resize' listener never calls it — it only re-measures terminal cols/rows. So when the keyboard opens, #app keeps its full height, the composer/prompt stays underneath the keyboard, and the user cannot see what they type. `applyViewport` is otherwise only called on orientationchange, keybar-toggle, and boot (lines ~846, ~856, ~1552, ~3079, ~3150) — never on keyboard show/hide.

FROZEN CONTRACT for defect 1:
- The visualViewport 'resize' handler MUST call applyViewport() (keep the existing debounced scheduleViewportFit(isMobile()?140:40) behavior for the cols/rows report — do not regress it).
- ALSO listen to visualViewport 'scroll' and call applyViewport(): iOS shifts the visual viewport rather than resizing the layout viewport, and only 'scroll' fires for that shift.
- applyViewport() must account for visualViewport.offsetTop, so the app stays pinned to the VISIBLE band on iOS (offsetTop > 0 while the keyboard is up). Pick your mechanism, but it must not break position:fixed children or introduce a containing block that breaks existing overlays/dialogs — say which you chose and why in AMBIGUITIES.
- Idempotent + cheap: these events fire continuously during keyboard animation. Do not write to the DOM when the computed height/offset is unchanged (memoise like applyScrollAffordance does).
- Desktop must be unaffected when there is no keyboard: with offsetTop 0 and a stable height, behavior is exactly as today.

=== DEFECT 2: every keybar tap is spent raising the keyboard, so nothing can be double-tapped ===
Current code (around line 3018):

function buildKeybar(){
  const kb=$('#keybar'); kb.innerHTML='';
  const mk=(label,cls,on)=>{ const b=document.createElement('button'); b.type='button'; b.className='key'+(cls?' '+cls:''); b.textContent=label; if(on) b.onclick=on; return b; };
  ... kb.appendChild(mk('?','accent', openHelp)); (many buttons; one of them, id="sizebtn", ALSO has
  touchstart/touchend/touchmove + mousedown/mouseup/mouseleave listeners implementing a 550ms long-press) ...

The buttons take focus away from xterm's hidden textarea on tap. The focus change is what toggles the soft keyboard, so the first tap is consumed by focus/keyboard and the action needs a second tap — making double-tap actions impossible.

FROZEN CONTRACT for defect 2:
- A tap on a #keybar button MUST NOT move focus away from the terminal's hidden textarea (that is what re-raises/toggles the keyboard).
- The button's click action MUST still fire on the FIRST tap, on both touch and mouse.
- The existing long-press on #sizebtn MUST still work (do not remove or reorder its listeners).
- Implement it ONCE at the #keybar container level (delegation), not per button, so custom user buttons added later inherit it.
- Beware: preventDefault on 'touchstart' suppresses the synthesized click on iOS — do not break click. State in AMBIGUITIES which event you prevented and why it preserves click.

=== GLOBAL CONSTRAINTS ===
- Comments explain WHY (the failure mode), never what the line does; match the file's existing dense-comment voice.
- No new dependencies, no framework, no build step. Vanilla only, matching the file.
- Touch nothing else in the file. Do not reformat, do not reorder unrelated code.

DELIVERABLE: Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\relay\public\index.html (edited in place — the only file you touch).
Run this yourself before reporting, from Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\relay:
VERIFY: node --test tests/client-layout.test.js tests/wheel-scroll.test.js tests/scroll-parity-web.test.js

Report PASS/FAIL, then an AMBIGUITIES section: every judgment call, especially the two called out above (offsetTop mechanism; which event you prevented for focus). Turn budget ~20 minutes.
