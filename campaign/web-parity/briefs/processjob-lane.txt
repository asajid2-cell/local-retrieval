SEALED BRIEF — fix two environment-fragile process-job tests (one file).

Context: a .NET 8 MSTest suite. Two tests in WindowsProcessJobTests have failed for weeks on this machine and were repeatedly dismissed as "pre-existing" and "load-sensitive". They are neither: the cause is now known exactly. Everything you need is below; no repo browsing required.

THE MEASURED CAUSE (do not re-derive):
Running `dotnet test` produces this failure:

  Failed Dispose_TerminatesAttachedProcessAndItsDescendant
  System.ComponentModel.Win32Exception: An error occurred trying to start process 'powershell.exe'
  with working directory
  'Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\app\native\CodexLocalRetrieval.Native.Tests\bin\Debug\net8.0-windows10.0.26100.0'.
  Access is denied.

The repository lives on Z:, a MAPPED NETWORK DRIVE. The tests inherit the test host's working
directory (the bin folder on Z:) when they start their powershell fixture processes, and Windows
refuses to start a process whose working directory is that mapped drive in this security context.
The production code under test is fine — it is the FIXTURE that is unlaunchable. The second failing
test, AbruptOwnerTermination_ClosesJobAndKillsContainedTree, fails downstream of the same problem
(its process probe returns one field instead of two because the child never started).

FROZEN CONTRACT:
- Every ProcessStartInfo the TESTS create for a fixture process must set an explicit, LOCAL
  WorkingDirectory — create a fresh directory under Path.GetTempPath() per test (Guid-named),
  and delete it in a finally/TestCleanup where a failed delete must not fail the test.
- Do NOT change production behavior to work around this. If you believe a production file must
  change, STOP and say so in AMBIGUITIES with the exact reason instead of changing it.
- Keep every existing assertion. The tests must still prove what they proved: that disposing the
  job terminates the attached process AND its descendant, and that abrupt owner termination closes
  the job and kills the contained tree.
- The fixture processes must remain windowless (there is already a -WindowStyle Hidden on the
  Start-Process calls — keep it; it exists because these fixtures were popping console windows).
- Add a short comment at each WorkingDirectory you set explaining WHY (a mapped network drive is
  not a legal working directory for a spawned process here), so nobody "cleans it up" later.

DELIVERABLE: Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\app\native\CodexLocalRetrieval.Native.Tests\WindowsProcessJobTests.cs (edited in place — the only file you touch)

Run this yourself before reporting, from Z:\328\CMPUT328-A2\codexworks\301\mux-local-retrieval\app\native:
VERIFY: dotnet test CodexLocalRetrieval.Native.Tests -c Debug --filter "FullyQualifiedName~WindowsProcessJobTests"

All 6 tests in that class must pass. Run it at least TWICE and report both results — these were
called "load-sensitive", so a single green run is not evidence. Report the pass/fail table, then an
AMBIGUITIES section listing every judgment call. Turn budget ~25 minutes.
