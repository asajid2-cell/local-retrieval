[
  {
    "title": "muxctl native wheel priority + scroll-parity vector file",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "In muxd/muxctl.py, make wheel_input_sequences (muxctl.py:196; alt-first branch :217) use real-terminal priority: mouse-tracking active -> wheel reports (SGR/urxvt/X10, even on the alt screen); bare alt screen WITH DECSET 1007 -> 3 arrows per notch (SS3 under DECCKM); bare alt WITHOUT 1007 -> page keys (existing 90ms rate limit) as the compatibility fallback; normal screen -> nothing. Add 1007 to ScreenModeTracker's tracked modes (:126 — 1007 is NOT tracked today). Default alt_scroll becomes 'sgr'; MUXCTL_WHEEL=pagekeys (wired at :660) stays as a forced override keeping today's alt-screen-first behavior. Create docs/scroll-parity.json (repo-root docs/ — create the dir): a vector table mapping every state {normal|alt} x {tracking on|off} x {1007 on|off} x {surface: muxctl|web} to exactly one mechanism, and rewrite the forwarding tests to assert every cell from that file (exactly-one-mechanism invariant included; existing tests assert the pagekeys-first order and MUST be rewritten against the vector file, not patched around). Also refresh the stale web-surface paragraphs of muxd/DEPLOY-local-scroll.md (it still claims the web strips ALL mouse reports; index.html:1527-1538 now keeps wheel). muxctl-only: takes effect on next attach, no daemon restart. Human-checklist: wheel-scroll a local codex attach, less pages once per notch, plain shell keeps conhost scrollback, Ctrl-] detach clean.",
    "verifier": "timeout 180 python -m pytest muxd/tests/test_local_scroll_forwarding.py muxd/tests/test_console_input_roundtrip.py muxd/tests/test_muxctl_input_mode.py -q",
    "reads": [
      {
        "path": "muxd/muxctl.py",
        "span": "1-781"
      },
      {
        "path": "muxd/tests/test_local_scroll_forwarding.py",
        "span": "1-457"
      },
      {
        "path": "muxd/tests/test_console_input_roundtrip.py",
        "span": "1-103"
      },
      {
        "path": "muxd/tests/test_muxctl_input_mode.py",
        "span": "1-41"
      },
      {
        "path": "muxd/DEPLOY-local-scroll.md",
        "span": "1-75"
      }
    ]
  },
  {
    "title": "web visible scrollbar + honest alt-screen scroll state",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Give the web viewer a visible themed scrollbar on .xterm-viewport in the normal buffer (scrollbar-width:thin + webkit rules, theme colors) plus a compact scroll-position readout co-located with the #jumplive pill, and in the alternate buffer hide the scrollbar and show an 'app scroll' state chip instead (a dead scrollbar is worse than none). Hook term.onScroll and buffer-type changes; reuse atBottom()/showJump() plumbing (index.html:1562-1591). Bump window.__muxBuild (:1015 — currently stale at 2026-07-12). Extend client-layout.test.js: (1) a .xterm-viewport scrollbar rule exists and is not display:none, (2) the alt-buffer toggle fn (vm-extracted, driven with fake term.buffer.active.type) shows chip + hides scrollbar, (3) position label math is a pure fn tested at top/middle/bottom. Human-checklist: scrollbar visible+draggable at ~1906x912, no touch-pan fight on phone, alt screen shows chip.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/client-layout.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "730-760"
      },
      {
        "path": "relay/public/index.html",
        "span": "1000-1020"
      },
      {
        "path": "relay/public/index.html",
        "span": "1550-1600"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "web selection/copy that feels native on desktop",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Desktop selection first-class in relay/public/index.html: drag-select survives streaming output (the autoFreeze latch exists at :982-990/:1133-1140 — harden it on onSelectionChange from mouse drags, not just explicit select mode), Ctrl/Cmd+Shift+C copies term.getSelection() with fallback to the copy overlay when the clipboard API rejects, auto-copy-on-select as a persisted LS toggle, Shift+drag documented in the help sheet for tracking sessions, and no contextmenu preventDefault over #term while a selection exists. Phone flow (selectMode + freeze + copy overlay, :2384-2389/:2574-2596) unchanged. Copy helpers today: :1125-1128, :2374-2377. Bump window.__muxBuild. NEW relay/tests/selection.test.js using the extractFn vm harness (wheel-scroll.test.js:14-26 pattern): (1) drag-latch freezes flush while a selection exists and flushes on clear, (2) copy handler routes selection -> stubbed clipboard writer, falls back to overlay on reject, (3) auto-copy toggle round-trips the LS shim, (4) section assertion: no contextmenu preventDefault over #term. Human-checklist: drag-select 30 lines while codex streams, Shift+drag in alt-screen codex, phone long-press unchanged.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/selection.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "970-1000"
      },
      {
        "path": "relay/public/index.html",
        "span": "1120-1210"
      },
      {
        "path": "relay/public/index.html",
        "span": "2370-2390"
      },
      {
        "path": "relay/public/index.html",
        "span": "2570-2600"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "restore conhost QuickEdit drag-select in the local attach",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "muxd/muxctl.py attach_input_mode (:54-62) clears ENABLE_QUICK_EDIT_MODE unconditionally, killing native drag-select for plain-shell local attaches. Restore QuickEdit whenever mouse capture is OFF (normal buffer, conhost owns wheel+selection) and clear it only while set_mouse_capture(True) is active (alt/tracking; :353, wired from wants_mouse_capture() in the attach loop :617-624); restore the original mode exactly on exit. QuickEdit pausing output during a drag is native local behavior and is desired. MUXCTL_VT_INPUT set -> VT input path unchanged (no QuickEdit). Test matrix over (capture on|off) x (vt_input on|off): QUICK_EDIT set exactly when capture off and vt_input off; MOUSE_INPUT set exactly when capture on; ENABLE_EXTENDED_FLAGS always; original mode restored on detach. Pure mode math, no console handle needed. Human-checklist: conhost drag-select+Enter copies from a plain shell attach; wheel still forwards inside codex; detach restores mode.",
    "verifier": "timeout 60 python -m pytest muxd/tests/test_muxctl_input_mode.py -q",
    "reads": [
      {
        "path": "muxd/muxctl.py",
        "span": "1-781"
      },
      {
        "path": "muxd/tests/test_muxctl_input_mode.py",
        "span": "1-41"
      }
    ]
  },
  {
    "title": "input->echo latency probe with enforced budgets",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Build scripts/latency_probe.py: open a throwaway muxd session running a pure echo child, send single-byte inputs over (a) the local muxd ws (ws://127.0.0.1:7699, MUXCTL_PORT env, same path muxctl uses) and (b) optionally the relay ws path gated behind MUXD_VPS_TESTS=1 (reuse muxd/tests/test_vps_smoke.py plumbing), measure input->matching-output RTT over >=200 samples, print p50/p95/p99, exit non-zero over budget (--budget-p95-ms, default 50 local / 120 relay). Hard --timeout wall clamp; always kill the probe session on every exit path. If muxd is unreachable, auto-start the MuxdSessionHost scheduled task exactly like muxctl does (muxctl.py:426-445) and exit 3 with a clear message if that fails. Measure-only: no tuning in this leaf (pump tuning stays deferred, gated on this probe's numbers). NEW muxd/tests/test_latency_probe.py unit-tests probe logic against a loopback fake (sample matching, percentile math, timeout kill).\n\nTRUST AMENDMENT: report a separate signed-path series using principal-auth.js, channel open, muxd signature verification, and signed output verification. The enforced path is the shipping path, so its p50/p95/p99 and budget are authoritative; unsigned measurements are diagnostic only.",
    "verifier": "timeout 180 python -m pytest muxd/tests/test_latency_probe.py -q && timeout 90 python scripts/latency_probe.py --local --samples 100 --budget-p95-ms 50 --timeout 60",
    "reads": [
      {
        "path": "muxd/muxctl.py",
        "span": "1-781"
      },
      {
        "path": "muxd/tests/test_vps_smoke.py",
        "span": "1-283"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3600-3680"
      }
    ]
  },
  {
    "title": "web click forwarding v2 with leak detector (mouse-report guard v2)",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Let alt-screen tracking apps receive discrete click/release SGR reports from the web surface (click-to-position/focus) without reopening the leaked-mode flood. In relay/public/index.html: add a small output-side mode tracker (mirror ScreenModeTracker semantics, muxctl.py:126-188: scan session output for DECSET/DECRST of 9/1000/1002/1003/1006 with a carried tail); stripMouseReports v2 (:1527-1538) passes SGR press/release reports (final M/m, Cb with neither bit 32 motion nor bit 64 wheel) ONLY while the tracker says tracking is active; motion reports are stripped unconditionally forever (the historic flood was motion reports echoed by a dead TUI's shell — guard comment :1522-1526). Leak detector: if session OUTPUT contains an echo of report bytes we forwarded within a short window, send DECRST 1000;1002;1003;1006 as input, stop forwarding, and re-arm only on the next DECSET seen in output. Bump window.__muxBuild. vm-harness tests: clicks pass when tracking-active, motion always stripped, echoed-report -> DECRST + drop + re-arm; wheel cells of docs/scroll-parity.json stay green untouched. Higher risk than other web leaves — schedule after web scroll parity (r.3 child 10) locks the parity tests if staggering; deploys/rollbacks are free this campaign.\n\nTRUST AMENDMENT: every forwarded click/release and every leak-detector DECRST control write is produced through principal-auth.js as signed input.raw for the current session/channel/lease epoch. No direct send('i', ...) bypass remains.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/wheel-scroll.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "1380-1560"
      },
      {
        "path": "relay/tests/wheel-scroll.test.js",
        "span": "1-59"
      },
      {
        "path": "muxd/muxctl.py",
        "span": "120-200"
      }
    ]
  },
  {
    "title": "fix superlinear input-filter freeze (takeTerminalReport OSC backtracking)",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "relay/public/index.html takeTerminalReport (:1424-1430) uses the OSC pattern \\x1b\\][0-9;]+;[^\\x07\\x1b]*(?:\\x07|\\x1b\\\\) which backtracks superquadratically on 'ESC ]' followed by a long unterminated [0-9;] run containing semicolons (measured on the real function: 20k->201ms, 40k->789ms, 80k->4.9s; paste-sized inputs freeze the UI thread for minutes). It runs per input chunk via stripTerminalGeneratedReports (:1432-1440) on term.onData (:1545) — every keystroke and paste. Replace with a linear-time scan (hand-rolled OSC scanner or an equivalent non-backtracking pattern; also skip non-ESC spans with indexOf instead of per-char slicing) with byte-identical filtering behavior on all well-formed reports. Bump window.__muxBuild. NEW relay/tests/input-filter.test.js (vm extractFn harness): (1) regression: strip of ESC ] + 200000 semicolons completes with the whole test file under the 20s timeout — the pathological input under the timeout IS the regression gate, (2) all current behavior preserved: well-formed DA/CPR/DSR/OSC reports stripped, plain text and non-report CSIs untouched, dropped flag correct, (3) unterminated OSC at end of chunk: pick and TEST a policy (recommend pass-through unchanged; document the choice in the test). This closes the campaign directive's 'hanging test' item: the directive's ~1432 anchor points at THIS function, not stripMouseReports; no test actually hangs today (relay.test.js just takes 72-78s total).",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/input-filter.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "1420-1560"
      },
      {
        "path": "relay/tests/wheel-scroll.test.js",
        "span": "1-59"
      }
    ]
  },
  {
    "title": "input continuity across ws reconnects",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Move the reconnect buffer into the trusted-client origin. Buffer captured plaintext only, capped at 4096 bytes and by each frame's original capture-time expiry; never store plaintext or signing authority at the relay. On reconnect, open and verify a fresh channel.accept, request/verify attach replay, then re-sign still-fresh captured bytes for the new channel while preserving original capturedAt/expiresAt. Expired bytes are dropped with a visible notice; reconnect cannot make stale input fresh. Flush after attach in order, stop on sequence/lease rejection, and never reuse the prior channel id or sequence. Non-input frames are not buffered. Compose closes only after sent/queued. Tests cover fresh-channel reset, prior-channel invalidation, original-expiry preservation, ordered flush, cap eviction, visible drops, no relay persistence, and no unsigned fallback.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/input-continuity.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "1170-1210"
      },
      {
        "path": "relay/public/index.html",
        "span": "1530-1600"
      },
      {
        "path": "relay/public/index.html",
        "span": "1680-1700"
      },
      {
        "path": "relay/public/index.html",
        "span": "2190-2210"
      },
      {
        "path": "relay/public/index.html",
        "span": "2350-2360"
      }
    ]
  },
  {
    "title": "terminal-model fidelity spike: pyte vs xterm-headless on real ConPTY streams",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Build muxd/spike/vt-fidelity/: (1) capture.py — records raw ring bytes from live muxd sessions over the local ws (t:'sb', max=800000, muxd.py:3823-3841) into fixture files; capture at least one alt-screen codex/claude session, one claude normal-buffer transcript, one plain shell; if no live TUI session exists on the runner, generate fallback fixtures by driving scripted TUIs (less on a large file, a python-curses demo) under ConPTY. (2) synthetic fixtures exercising every REPLAY_PRIVATE_MODES mode (1,47,1047,1049,2004,9,1000,1002,1003,1005,1006,1007,1015 — muxd.py:920) plus DECAWM wrap, SGR 256/truecolor, ED/EL, DECSTBM regions, CUP/CUU/CUD, DECCKM, OSC 0/2, UTF-8 wide chars/emoji — the ConPTY dialect muxd actually replays. (3) harness.mjs + a python shim — feed each fixture to BOTH candidates: pyte (HistoryScreen; importable in the repo Python) and @xterm/headless (NOT in relay/node_modules — add as devDependency); diff final visible screen text + cursor against @xterm/headless as oracle; run a REFLOW probe per fixture: candidate ingests at 140 cols, resizes to 80, serializes; compare against xterm-headless fed the same stream then resized to 80. (4) emit report.json: per-fixture per-candidate cell-fidelity %, reflow-fidelity %, ingest throughput (bytes/s), and a computed go/no-go per candidate. GO means: >=99.5% cell fidelity on synthetic corpus AND >=99% on recorded corpus AND >=98% reflow fidelity AND throughput >= 5MB/s. The verifier proves the harness ran and produced a complete report — a no-go candidate does NOT fail this leaf; known asymmetry to surface honestly with numbers: pyte does not reflow wrapped lines on resize while xterm-headless reflows natively and its SerializeAddon emits replayable VT. Fixtures containing recorded real session content: flag for the orchestrator before committing.",
    "verifier": "timeout 270 node muxd/spike/vt-fidelity/harness.mjs --corpus muxd/spike/vt-fidelity/fixtures --out muxd/spike/vt-fidelity/report.json && python -c \"import json;r=json.load(open('muxd/spike/vt-fidelity/report.json'));assert r.get('fixtures') and all('go' in c for c in r['candidates'].values())\"",
    "reads": [
      {
        "path": "muxd/muxd.py",
        "span": "890-930"
      },
      {
        "path": "muxd/muxd.py",
        "span": "1240-1300"
      },
      {
        "path": "muxd/muxd.py",
        "span": "1530-1560"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3600-3680"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3810-3870"
      },
      {
        "path": "relay/package.json",
        "span": "1-17"
      },
      {
        "path": "muxd/tests/test_local_scroll_forwarding.py",
        "span": "1-457"
      }
    ]
  },
  {
    "title": "web scroll parity locked to the shared vector file",
    "kind": "build",
    "tier": "default",
    "deps": [
      "1"
    ],
    "goal": "Extend relay/tests/wheel-scroll.test.js (+ client-layout.test.js section assertions) to load docs/scroll-parity.json (created by the muxctl wheel leaf) and fail if the shipped web behavior disagrees with any web-surface wheel cell: normal buffer -> capture router scrolls xterm scrollback and stops the event; alt buffer -> trusted wheel forwarded to xterm (app receives SGR report when tracking, xterm alternateScroll arrows when 1007, nothing when bare); synthetic events rejected (isTrusted, router :1393-1419); no recursion. Document/assert the touch path separately (scrollAlternate page keys, 90ms limit, index.html:1282-1287) as the touch-surface cell. Only touch relay/public/index.html if an assertion exposes a real divergence; then bump window.__muxBuild (currently stale at 2026-07-12, :1015). Reuse the extractFn vm harness (wheel-scroll.test.js:14-26) and the section() harness (client-layout.test.js:8-14). If xterm.js's bare-alt+1007 arrow behavior can't be asserted headlessly, assert the router's forwarding contract and record the 1007 cell as covered by the vt-fidelity spike rig. Pinned policy: real-terminal semantics win — if the web client diverges from a vector cell, patching the web client is in-scope for this leaf.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/wheel-scroll.test.js relay/tests/client-layout.test.js",
    "reads": [
      {
        "path": "relay/tests/wheel-scroll.test.js",
        "span": "1-59"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      },
      {
        "path": "docs/scroll-parity.json",
        "estTokens": 3000
      },
      {
        "path": "relay/public/index.html",
        "span": "1380-1420"
      },
      {
        "path": "relay/public/index.html",
        "span": "1520-1540"
      }
    ]
  },
  {
    "title": "judge: pin the terminal-model architecture from spike evidence",
    "kind": "judge",
    "tier": "deep",
    "deps": [
      "9"
    ],
    "goal": "Read muxd/spike/vt-fidelity/report.json and the spike fixtures/harness, then write campaign/research/decisions/nat-h-model.md pinning: EMULATOR: (pyte in-proc | xterm-headless node sidecar | other), SNAPSHOT_FORMAT: (serialized VT bytes | JSON grid) for the snapshot/sbtext caps, REFLOW: how attach-repaint reflow is produced (native emulator reflow vs model-side logical-line tracking), MEMORY_BOUND: the per-session cap and trim policy consistent with muxd/PLAN.md's bounded-resources contract (its Responsiveness section: ring access synchronized, queues bounded, output bounded and drained), and PROCESS_MODEL: in-process vs one shared sidecar process (weigh ops cost on the muxd restart discipline). Justify each against the report's numbers; a candidate that failed reflow cannot be chosen for attach-repaint duty unless the decision adds a compensating design. Everything downstream (model sidecar, attach repaint, sbtext) consumes this contract.\n\nAdditionally pin BACKPRESSURE_RESYNC: the resync payload a desynced viewer receives - model snapshot in the pinned SNAPSHOT_FORMAT for flag-on sessions with a live model; raw-ring sb replay otherwise - with candidate-specific bounds. The policy core is fixed: never block the PTY, and never silently drop without a forced resync plus visible gap marker.",
    "verifier": "python -c \"import re;s=open('campaign/research/decisions/nat-h-model.md',encoding='utf-8').read();assert all(re.search(k+r':\\s*\\S+',s) for k in ('EMULATOR','SNAPSHOT_FORMAT','REFLOW','MEMORY_BOUND','PROCESS_MODEL','BACKPRESSURE_RESYNC'))\"",
    "reads": [
      {
        "path": "muxd/spike/vt-fidelity/report.json",
        "estTokens": 6000
      },
      {
        "path": "campaign/mux-native.md",
        "span": "1-290"
      },
      {
        "path": "muxd/PLAN.md",
        "span": "1-91"
      },
      {
        "path": "muxd/muxd.py",
        "span": "1240-1300"
      }
    ]
  },
  {
    "title": "muxd terminal-model sidecar + snapshot capability (feature-flagged)",
    "kind": "build",
    "tier": "default",
    "deps": [
      "11"
    ],
    "goal": "Per the pinned contract in campaign/research/decisions/nat-h-model.md, add a per-session terminal model to muxd/muxd.py fed from the session output tap (Session output path :1540-1543 beside replay_state.ingest; OwnerSession (:1735) duplicates the ring code at :1793/:1842 and needs the same tap), behind env flag MUXD_TERM_MODEL (default OFF -> byte-identical behavior everywhere). Enforce the pinned per-session memory bound (trim history on breach, never crash, expose a gauge in session info). Add a 'snapshot' ws capability beside 'sb' in BOTH handlers (local :3626-3668, relay :3819-3859): request {t:'snapshot',s,rid} -> response {t:'snapshot',rid,cols,rows,modes,d:<pinned format>}. NO attach-path change in this leaf — attach replay stays raw-ring. Advertise the capability in the protocol info so clients can feature-detect. NEW muxd/tests/test_terminal_model.py: (1) flag OFF -> attach_replay_payload and scrollback byte-identical to today (golden), (2) flag ON -> snapshot of a fed fixture matches the emulator oracle, (3) memory gauge respects the pinned cap under a RING_CAP-sized flood, (4) model death/lag never blocks the output pump (tap must be non-blocking; drop-model-not-output). If the decision pins a node sidecar process, tests stub the transport and one integration test drives the real child. Deploys are free (scripts/deploy-muxd.ps1 + MuxdSessionHostRestart).\n\nFLAG FREEZE: MUXD_TERM_MODEL is evaluated once at session creation and stamped immutable as model_enabled; flag flips affect new sessions only. snapshot/sbtext/digest on a frozen-off session return model-off for that session lifetime, and protocol/session info reports per-session state. ORDERING: snapshot and digest are captured under the session output lock and emitted through the per-session ordered output path. Tests add off-then-flip refusal with zero ingest taps, mixed-population visibility, and concurrent-flood snapshot-then-raw oracle equivalence. TRUST: snapshot/digest responses use muxdSignedOutputV1 and are verified before render.",
    "verifier": "timeout 270 python -m pytest muxd/tests/test_terminal_model.py muxd/tests/test_muxd_state.py -q",
    "reads": [
      {
        "path": "campaign/research/decisions/nat-h-model.md",
        "estTokens": 3000
      },
      {
        "path": "muxd/muxd.py",
        "span": "1530-1560"
      },
      {
        "path": "muxd/muxd.py",
        "span": "1730-1850"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3600-3680"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3810-3870"
      },
      {
        "path": "muxd/PLAN.md",
        "span": "1-91"
      },
      {
        "path": "muxd/tests/test_muxd_state.py",
        "span": "91-270"
      }
    ]
  },
  {
    "title": "resize-correct attach repaint from the model snapshot",
    "kind": "build",
    "tier": "default",
    "deps": [
      "12"
    ],
    "goal": "When MUXD_TERM_MODEL is on and a viewer attaches with cols/rows differing from the width the ring bytes were written at, serve the attach replay from the model (reflowed to the session's current size per the pinned REFLOW strategy) instead of raw ring bytes — killing the wrapped-line garble that raw byte replay produces on width change (ring stores raw bytes wrapped at write-time width; scrollback() :1694-1701 replays them verbatim). Extend attach_replay_payload (muxd.py:1248) and its call sites (local attach :3636-3641, relay 'sb' op :3823-3841) with the viewer-dims decision; the raw ring path remains the fallback whenever the flag is off, the model is dead/lagging, or sizes match. redraw_nudge (:1257) stays for alt-screen truth. NEW muxd/tests/test_attach_repaint.py: write wrapped-line fixture at 140 cols, attach at 80 -> replay rendered through the emulator at 80 equals the oracle text (and a control test proving the raw path DOES garble, so the win is demonstrated not assumed); same-width attach -> byte-identical raw replay (no regression); model-fallback path exercised. Alt-screen sessions: replay mode-prefix + nudge as today — the model repaint targets the NORMAL buffer per the alt-screen doctrine.\n\nTRUST AMENDMENT: attach repaint is a muxd-signed, request-nonce-bound response. The trusted-origin client verifies the signature/checkpoint before total replacement; relay-provided or replayed repaint data is never rendered.",
    "verifier": "timeout 270 python -m pytest muxd/tests/test_terminal_model.py muxd/tests/test_attach_repaint.py -q",
    "reads": [
      {
        "path": "campaign/research/decisions/nat-h-model.md",
        "estTokens": 3000
      },
      {
        "path": "muxd/muxd.py",
        "span": "1240-1300"
      },
      {
        "path": "muxd/muxd.py",
        "span": "1680-1710"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3620-3650"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3810-3850"
      },
      {
        "path": "muxd/tests/test_terminal_model.py",
        "estTokens": 8000
      }
    ]
  },
  {
    "title": "sbtext capability: rendered history text export",
    "kind": "build",
    "tier": "default",
    "deps": [
      "12"
    ],
    "goal": "Add an 'sbtext' ws capability beside 'sb'/'snapshot' in both muxd handlers (local :3626-3668, relay :3819-3859): request {t:'sbtext',s,lines:N,rid} -> {t:'sbtext',rid,d:<rendered plain-text history lines, newest last>} served from the terminal model's history+screen (bounded: N clamped to the model's retained history, response size capped consistent with muxd/PLAN.md bounds). This exports RENDERED lines (what a scrollback pager would show), not raw VT bytes — the contract the web history/search overlay and future muxctl copy/search consume. Normal-buffer history only; alt-screen yields the current screen text plus any normal-buffer history (doctrine: no fake alt transcript). Value beyond xterm's own scrollback: exports history past the attach-replay window (full RING_CAP-worth, rendered) and serves surfaces that never replayed it. NEW muxd/tests/test_sbtext.py: rendered output matches emulator oracle for a wrapped+SGR fixture (attributes stripped, wide chars intact), clamping honored, flag-off -> capability absent from protocol info.\n\nTRUST AMENDMENT: sbtext responses are muxd-signed and nonce-bound. The trusted-origin client verifies before display; relay plaintext exposure remains accepted.",
    "verifier": "timeout 180 python -m pytest muxd/tests/test_sbtext.py -q",
    "reads": [
      {
        "path": "campaign/research/decisions/nat-h-model.md",
        "estTokens": 3000
      },
      {
        "path": "muxd/muxd.py",
        "span": "3600-3680"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3810-3870"
      },
      {
        "path": "muxd/tests/test_terminal_model.py",
        "estTokens": 8000
      }
    ]
  },
  {
    "title": "web history/search overlay over sbtext",
    "kind": "build",
    "tier": "default",
    "deps": [
      "14"
    ],
    "goal": "Add a history/search overlay to relay/public/index.html: a keybar/header affordance opens a frozen overlay pane (pattern: the existing copy-overlay <pre>, :2574-2596) that requests sbtext from muxd (feature-detected via protocol info; hidden when absent), renders the tail, supports incremental text search with match highlighting and prev/next jumps within the overlay, and native selection/copy inside it. The overlay is read-only and separate from the live xterm viewport (no fake scrollback injection into xterm — alt-screen doctrine). Bump window.__muxBuild. NEW relay/tests/history-overlay.test.js (vm extractFn + section harness): search fn is pure and tested (case-insensitive, match indexing, prev/next wrap), overlay open/close never touches term input state, feature-detect hides the affordance when sbtext is absent, request uses rid correlation. SEAM: same file as the scrollbar/selection leaves and the value prong's UI work — coordinate the keybar/help-sheet region at merge; adversarial lane owns ws frame validation.\n\nTRUST AMENDMENT: the overlay consumes only verified muxd-signed sbtext responses from the trusted origin. A relay-supplied payload or success state is rejected.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/history-overlay.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "560-600"
      },
      {
        "path": "relay/public/index.html",
        "span": "2570-2600"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "relay lease UX mirror over muxd-authoritative signed write lease",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Implement only the relay/browser presentation layer for trust leaf 5. relay/server.js forwards signed input and signed lease.steal frames unchanged and caches muxd-signed lease notices only as advisory UI state; it never accepts, drops, transfers, refreshes, or steals as the authority. relay/public/input-lease.js verifies/consumes trusted lease state and renders the read-only banner plus one-tap signed takeover. Deliberate /send and send-when-idle operations already carry signed lease behavior and cannot be minted by the relay. Two-viewer tests with fake muxd prove A acquires, B sees signed lease-held, B signed-steals, delayed A old-epoch input is rejected by muxd, forged relay holder metadata is ignored, and FakeHost records no interleaving. Local muxctl remains a separately authenticated recovery principal.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=60000 tests/input-lease.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "2250-2335"
      },
      {
        "path": "relay/public/index.html",
        "span": "1170-1210"
      },
      {
        "path": "relay/public/principal-auth.js",
        "estTokens": 7000
      },
      {
        "path": "relay/tests/harness.js",
        "estTokens": 7000
      }
    ]
  },
  {
    "title": "viewer backpressure: bounded per-viewer queue, drop-and-resync, never block the PTY",
    "kind": "build",
    "tier": "default",
    "deps": [
      "12"
    ],
    "goal": "Implement the pinned backpressure policy at the relay fan-out. relay/server.js: per-viewer bounded outbound budget (ws bufferedAmount + queued bytes, cap MUX_VIEWER_BUF_CAP default 1MB, injectable small in MUX_TEST_MODE); on breach stop queueing raw output for THAT viewer only and mark it desynced; the host link and other viewers are never paused. On drain: emit a rid-correlated resync BEFORE any newer raw output — request the muxd snapshot when the session advertises a live model (caps from r.3 child 12), else the existing raw-ring 'sb' replay. Client side (relay/public/index.html terminal-behavior block + relay/public/resync.js, vm-testable): while desynced, discard incoming raw frames until the rid'd resync arrives; apply snapshot as TOTAL replacement (term.reset + write, never merge) or replay the sb payload; insert a visible gap-marker line into scrollback on every resync so the dropped window is represented; bump window.__muxBuild. The reliability batch's backpressure characterization tests are the current-behavior baseline — coordinate supersession at merge (orchestrator seam); do not co-edit that batch. Tests (tests/backpressure.test.js, harness + FakeHost): (1) viewer's raw TCP socket paused, FakeHost floods 5MB -> relay keeps consuming host frames at full rate (processed-frame count grows; FakeHost sees no stall) while the paused viewer's queue stays under cap (gauge exposed in /api/health under MUX_TEST_MODE); (2) socket resumed -> viewer receives resync payload BEFORE any post-flood raw bytes, then live bytes; (3) a second healthy viewer receives the entire flood uninterrupted throughout; (4) vm test of resync.js: desynced state drops raw until the rid matches, applies reset+write, emits exactly one gap marker per episode.\n\nTRUST AMENDMENT: resync requests carry a fresh client nonce; snapshot/raw-ring payloads and the resync checkpoint are muxd-signed. The client remains desynced and discards raw frames until the signature, nonce, stream epoch, and sequence checkpoint verify. The relay may drop but cannot fabricate recovery.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=60000 tests/backpressure.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "1800-1840"
      },
      {
        "path": "relay/public/index.html",
        "span": "1170-1210"
      },
      {
        "path": "relay/tests/harness.js",
        "estTokens": 7000
      },
      {
        "path": "campaign/research/decisions/nat-h-model.md",
        "estTokens": 3000
      }
    ]
  },
  {
    "title": "screen-state divergence detector: client-vs-model digest with self-heal",
    "kind": "build",
    "tier": "default",
    "deps": [
      "12"
    ],
    "goal": "Pin ONE canonical screen digest for every interpreter: visible rows as plain text, trailing spaces trimmed, trailing blank rows dropped, rows joined with '\\n', UTF-8, sha256 hex; cursor {row,col} carried alongside. Attributes are excluded — text+cursor is the honesty floor; attribute fidelity stays covered by the spike corpus percentages. muxd: {t:'digest',s,rid} -> {t:'digest',rid,hash,cursor} served from the terminal model, flag-on sessions only, capability advertised beside 'snapshot', response emitted through the same ordered output path. Client: relay/public/divergence.js with a pure digestFromTerm(term) over xterm's buffer API + index.html wiring in the terminal-behavior block; when the digest cap is present, compare on a slow idle cadence (default every 30s of viewer idle, env MUX_DIVERGE_CHECK_MS, shrinkable in test) and IMMEDIATELY after every resync event; on mismatch increment window.__muxDivergence, log both hashes + cursors, and self-heal via a snapshot repaint (total replacement per the pinned reconciliation protocol); >3 mismatches in 10 minutes surfaces a persistent status chip instead of silent repaint-thrashing. Bump window.__muxBuild. Cross-interpreter fixtures: every muxd/spike/vt-fidelity fixture gains an expected-digest file; the node test computes digests via @xterm/headless and the python test via the pinned model emulator, BOTH must reproduce the expected values — corpus-level agreement is proven, the runtime detector covers everything the corpus misses. Tests: node — digestFromTerm deterministic over a fake buffer, fixture digests match expected, mismatch path triggers exactly one (stubbed) snapshot repaint, chip threshold logic pure-tested; python — model digest matches expected per fixture, digest of a flag-off session refused.\n\nTRUST AMENDMENT: digest and corrective snapshot are muxd-signed and request-nonce-bound; the trusted-origin client verifies both before comparison or repaint. A relay-provided digest is never authoritative.",
    "verifier": "node --test --test-force-exit --test-timeout=20000 relay/tests/divergence.test.js && timeout 120 python -m pytest muxd/tests/test_model_digest.py -q",
    "reads": [
      {
        "path": "campaign/research/decisions/nat-h-model.md",
        "estTokens": 3000
      },
      {
        "path": "muxd/muxd.py",
        "span": "3600-3680"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3810-3870"
      },
      {
        "path": "relay/public/index.html",
        "span": "1530-1600"
      },
      {
        "path": "muxd/spike/vt-fidelity/fixtures",
        "estTokens": 8000
      }
    ]
  }
]
