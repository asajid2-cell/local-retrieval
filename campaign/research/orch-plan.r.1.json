[
  {
    "title": "Host-link protocol field/cap registry + conformance test",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Create docs/protocol-fields.md: a registry of every muxd<->relay ws frame type, field, and capability string, seeded from the current tree (muxd.py PROTOCOL=4/CAPS at :906-907, server.js REQUIRED_HOST_PROTOCOL/REQUIRED_HOST_CAPS at :163 and :265-270, hello/sessions/createResult/sb/o frame shapes). Registry rule stated in the doc: campaign leaves add FIELDS and CAPS additively and never bump the protocol number (relay requires exact protocol equality, so a bump bricks the pairing until both sides deploy). Add relay/tests/protocol-registry.test.js (node:test, static-read style of tests/client-layout.test.js) asserting: every cap literal in muxd.py CAPS and every entry in server.js REQUIRED_HOST_CAPS appears in the registry doc, and the registry's protocol number matches both sides. This leaf OWNS docs/protocol-fields.md; the custody-janitor, agent-truth, and output-fairness leaves append entries to it, and the native prong's terminal-model capabilities (snapshot/sbtext/input) must be registered here too.\n\nTRUST AMENDMENT: register principalAuthV1, principalAclV1, muxdSignedOutputV1, muxdWriteLeaseV1, deferredInputV1, all signed request/response/error shapes, and the protocol-4 additive/no-downgrade rule. Registry entries describe wire compatibility; they do not confer authorization.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/protocol-registry.test.js",
    "reads": [
      {
        "path": "muxd/muxd.py",
        "span": "1-60"
      },
      {
        "path": "muxd/muxd.py",
        "span": "900-910"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3520-3540"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3720-3740"
      },
      {
        "path": "relay/server.js",
        "span": "140-290"
      },
      {
        "path": "relay/server.js",
        "span": "1770-1860"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "Launch-claim sweeper in muxd",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Add sweep_launch_claims() to muxd: on boot and on a divided status-pump cadence (~60s), delete claim files under CLAIM_ROOT (muxd.py:900) whose self-described ExpiresUtc <= now, or whose OwnerProcess=='muxd' with a dead owner pid; quarantine unparseable claims by renaming to .claim.bad (never delete them); never touch a claim younger than its TTL even if the owner is unverifiable. Zero app-side edits (SessionLaunchClaims.cs belongs to the app lane; claim files are self-describing so the muxd sweeper covers the shared directory). Extend muxd/tests/test_muxd_state.py with claim-sweep tests: expired deleted; live-owner unexpired kept; dead-muxd-pid deleted; malformed quarantined; sweep idempotent.",
    "verifier": "cd muxd && timeout 120 python -m unittest tests.test_muxd_state -k claim -v",
    "reads": [
      {
        "path": "muxd/muxd.py",
        "span": "895-1160"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3700-3740"
      },
      {
        "path": "muxd/tests/test_muxd_state.py",
        "span": "1-300"
      }
    ]
  },
  {
    "title": "Burst/backpressure regression drill (tests only)",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Lock in the existing viewer-backpressure fix as a reproduced-failure suite; NO server.js changes (if a live defect surfaces, report it upward as a new leaf instead of patching in place). New relay/tests/burst-backpressure.test.js using the relay.test.js MUX_STATE_DIR + fake /host ws harness: (a) flood one session >=16MiB with a never-reading slow viewer attached -> slow viewer is TERMINATED (not gapped), fast viewer receives a byte-complete stream (sendViewer server.js:2116-2132); (b) burst arriving while scrollback is pending -> queue-flush path emits CLEAR + full replay in order (:1867-1899); (c) replay burst just under/over VIEWER_REPLAY_BURST_BYTES (:2050) -> boundary behavior asserted. Keep the suite <60s wall.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=90000 tests/burst-backpressure.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "1860-1910"
      },
      {
        "path": "relay/server.js",
        "span": "2040-2160"
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      }
    ]
  },
  {
    "title": "SIGTERM deploy drain + client restarting toast",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Deploys become invisible: server.js (which today has NO signal handler — grep confirms) gains a SIGTERM path that stops accepting upgrades, closes viewer sockets with code 1012 reason 'restarting' (host link closed last), flushes durable state, and hard-exits within 5s (setTimeout(...,5000).unref()). Client index.html maps close code 1012 in its onclose/closeRefusal path (closeRefusal at :1614, used at :1680) to an immediate 500ms-backoff reconnect plus a 'relay restarting...' toast via existing flash() (:1999). scripts/deploy-relay.sh (repo-root scripts/, NOT relay/scripts/) restarts SIGTERM-first. New relay/tests/drain.test.js: spawn server as a child process with MUX_STATE_DIR sandbox, attach viewer + fake host ws, send SIGTERM -> (a) viewer gets close 1012 within 2s, (b) process exits 0 within 5s, (c) post-SIGTERM upgrade attempt refused; plus one static-source assertion (client-layout.test.js style) that index.html handles 1012.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/drain.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "1-80"
      },
      {
        "path": "relay/server.js",
        "span": "2040-2140"
      },
      {
        "path": "relay/public/index.html",
        "span": "1600-1700"
      },
      {
        "path": "relay/public/index.html",
        "span": "1990-2005"
      },
      {
        "path": "scripts/deploy-relay.sh",
        "span": "1-8"
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "Relay lifecycle regression drill: create-confirm + kill-confirm (fixed — lock it)",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "The lifecycle-races defect was fixed by the integration merge: creates resolve only on validated createResult via pendingHostCreates (server.js:169, :226-263, :1832-1850) and DELETE /api/sessions waits for muxd removal confirmation (:699-710). Turn that into a permanent tests-only drill in relay/tests/lifecycle-confirm.test.js: (a) POST /api/sessions with a host that never sends createResult -> request fails after timeout AND /api/sessions lists no phantom row; (b) kill confirmed by host removal -> row gone; replaying a pre-kill 'sessions' frame naming the dead session then a corrected frame -> steady state stays dead; (c) createResult with mismatched name identity is rejected (:1837-1841); (d) host-link drop fails all pending creates (:1919-1921). NO production changes expected; if the drill finds a residual race, file it upward as a new fix leaf.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/lifecycle-confirm.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "160-270"
      },
      {
        "path": "relay/server.js",
        "span": "690-715"
      },
      {
        "path": "relay/server.js",
        "span": "1770-1930"
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      }
    ]
  },
  {
    "title": "Intent-journal coverage residue: kill/autoheal/rename/uploads",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "The merge already routed create/relaunch/rename-via-app-command/settabcolor/addtocollection/fetchfile through postIntent (index.html:696 loads intent-journal.js). Close the remainder: autoheal POST (index.html:1977), kill DELETE (:1993, :2066), session rename PATCH (:2180), uploads keep PATCH (:2321) and DELETE (:2322). Extend relay/public/intent-journal.js with a method-aware sendIntent(method,url,payload,prefix) (POST-only today, :79-99), and make the relay routes for these mutations accept+dedupe an intentId (kill/autoheal/rename in server.js :650-729; uploads :1704-1729) following the existing app-commands dedup pattern (relay.test.js:612-705). Keep UX (flash/confirm) unchanged; read-only GETs stay raw; the raw byte-upload POST (:2287) stays raw but gets an allowlist entry + comment. New relay/tests/intent-coverage.test.js: (a) static-source allowlist assertion that index.html contains no raw mutating fetch() outside the allowlist; (b) integration: same kill/autoheal/rename/upload-delete replayed with one intentId -> exactly one execution.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/intent-coverage.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "690-700"
      },
      {
        "path": "relay/public/index.html",
        "span": "1970-2000"
      },
      {
        "path": "relay/public/index.html",
        "span": "2170-2190"
      },
      {
        "path": "relay/public/index.html",
        "span": "2280-2330"
      },
      {
        "path": "relay/public/intent-journal.js",
        "span": "1-103"
      },
      {
        "path": "relay/server.js",
        "span": "640-730"
      },
      {
        "path": "relay/server.js",
        "span": "1700-1730"
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "600-710"
      }
    ]
  },
  {
    "title": "Glyph-blank recovery browser harness",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Give the Chromium glyph-blank recovery path (terminalPaintLooksBlank index.html:797, recoverTerminalPaint :818 with clearTextureAtlas, schedulePaintHeal :842) a real-browser test so layout refactors cannot silently break it. New relay/tests/browser/glyph-blank.spec.js (Playwright, headless Chromium) + tiny static-serve/stub-ws fixture: fill the terminal via stub ws, toggle the display:none layout paths (mobile sessions view / projects overlay classes — re-locate, base-plan anchors drifted), toggle back, then assert within 2s that the page's own terminalPaintLooksBlank() returns false AND direct canvas pixel readback is non-blank; also assert repeated toggles do not wedge the recovery throttle. Expose the needed hooks under a window.__muxTest shim only. The test asserts the RECOVERY INVARIANT (post-relayout repaint is non-blank), which regresses if the heal path breaks regardless of whether the upstream Chromium bug fires headlessly. Add devDependency playwright + npm run test:browser in relay/package.json — kept OUT of npm test so the deploy gate stays dependency-light. package.json scripts seam with the npm-gate leaf: additive keys only.",
    "verifier": "cd relay && npx playwright test tests/browser/glyph-blank.spec.js --reporter=line --timeout=30000",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "750-980"
      },
      {
        "path": "relay/package.json",
        "span": "1-17"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "Off-box relay state backups",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Relay durable state (projects.json, app-commands.json, pins.json, uploads-meta.json, rename-intents.json and .bak siblings under the relay state dir) survives VPS loss. New scripts/backup-state.sh (repo-root scripts/, beside deploy-relay.sh): tar the state files and scp to the PC over the existing 'win' ssh route; supports --dry-run-to <dir> (local archive, no scp) and --verify (restore-roundtrip against the local archive). Committed systemd-timer unit under relay/ops/. Installation on the VPS is an ops step for the deploy owner — this leaf delivers script + unit + test. Ship a tiny fixture state dir under relay/tests/fixtures/backup-state so the verifier is hermetic.\n\nTRUST AMENDMENT: explicitly exclude muxd identity keys, client private keys, local-control credentials, principal registries, and session ACL state from relay backups. The backup destination inherits the relay exposure profile for plaintext transcripts, archive indexes, signed intent envelopes, metadata, and timing; document that exposure in the restore test fixture.",
    "verifier": "cd relay && bash ../scripts/backup-state.sh --dry-run-to /tmp/mux-backup-test --state-dir tests/fixtures/backup-state && tar -tzf /tmp/mux-backup-test/*.tgz | grep -q pins.json",
    "reads": [
      {
        "path": "scripts/deploy-relay.sh",
        "span": "1-8"
      },
      {
        "path": "relay/durable-state.js",
        "span": "1-207"
      },
      {
        "path": "relay/server.js",
        "span": "720-760"
      }
    ]
  },
  {
    "title": "uploadId token grammar + resolved-path containment",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "RemoteUploadTransfer.FetchAndInsertAsync (app/native/CodexLocalRetrieval.Core/Remote/RemoteUploadTransfer.cs) accepts any nonblank uploadId (:24) and uses it raw in Path.Combine(destRoot, uploadId) (:31) and in the scp remote path (:43), so '..\\outside' escapes the upload root. Enforce a strict token grammar (e.g. ^[A-Za-z0-9._-]{1,64}$, no leading dot) AND a resolved-path containment check (Path.GetFullPath(destDir) must be strictly under Path.GetFullPath(destRoot)) before either use; refuse with a clear Detail string otherwise. Mirror the relay-side upload id grammar if one exists (check server.js /api/upload id minting) — the bridge must not trust the relay-visible id (defense at the executing end). Extend the existing app/native/CodexLocalRetrieval.Native.Tests/RemoteUploadTransferTests.cs: traversal IDs ('..', '..\\x', absolute, drive-qualified, UNC, 260-char, empty-after-trim) are refused before any filesystem/scp side effect; a plain token still resolves inside the root.\n\nTRUST AMENDMENT: this leaf proves path containment only. A remote upload still requires an upload.fetch principal proof verified by the desktop bridge in trust leaf 11; uploadId grammar, replayPolicy, leaseToken, and intentId are not authorization.",
    "verifier": "cd app/native && dotnet test CodexLocalRetrieval.Native.Tests/CodexLocalRetrieval.Native.Tests.csproj -c Release --filter \"FullyQualifiedName~RemoteUploadTransferTests\" --blame-hang --blame-hang-timeout 3m",
    "reads": [
      {
        "path": "app/native/CodexLocalRetrieval.Core/Remote/RemoteUploadTransfer.cs",
        "span": "1-153"
      },
      {
        "path": "app/native/CodexLocalRetrieval.Native.Tests/RemoteUploadTransferTests.cs",
        "span": "1-67"
      }
    ]
  },
  {
    "title": "Enforce replay lease+intent envelope for intent-fenced bridge commands",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "Intent-fenced commands (fetchfile, startmux) currently execute after only a type/replayPolicy check: RemoteCommandProtocol.IsReplaySafe (RemoteCommandProtocol.cs:19-36) is the sole gate in both pollers (MainPage.Remote.cs:387; RemoteBridge.cs:180), deserialized leaseToken/intentId default to empty, acks echo the possibly-empty leaseToken (MainPage.Remote.cs:474; RemoteBridge.cs:236), and a missing command intent is replaced by a freshly minted local intent (MainPage.Remote.cs:1091-1093) which breaks redelivery dedup. Add a protocol-level envelope check (e.g. RemoteCommandProtocol.RequiresIntentEnvelope(type) + ValidateEnvelope(intentId, leaseToken)): for intent-fenced types, a nonblank well-formed leaseToken AND a stable nonblank intentId are required BEFORE any side effect in BOTH pollers; violations ack as failed without executing; the local-intent-minting fallback applies only to genuinely GUI-originated starts, never to polled remote commands. Add tests to RemoteCommandProtocolTests.cs plus poller-level tests (RemoteApiTests.cs style): missing/blank lease or intent -> refused, no side effect, failure ack; valid envelope -> executes once; same intentId redelivered -> deduped. Also add one regression assertion pinning the fetchfile contract: an insert-mode-less fetchfile returns the status Detail WITHOUT prompt insertion (RemoteUploadTransfer.cs:88-90).\n\nTRUST AMENDMENT: a correct replayPolicy proves operation semantics, while leaseToken and intentId prove currency/deduplication; none proves who authorized the operation. Keep this leaf as the replay/work-distribution layer. Trust leaf 11 later requires and verifies the enqueuing principal proof before every PC-side effect, and removes remote local-intent minting.",
    "verifier": "cd app/native && dotnet test CodexLocalRetrieval.Native.Tests/CodexLocalRetrieval.Native.Tests.csproj -c Release --filter \"FullyQualifiedName~RemoteCommandProtocol|FullyQualifiedName~RemoteApi\" --blame-hang --blame-hang-timeout 3m",
    "reads": [
      {
        "path": "app/native/CodexLocalRetrieval.Core/Remote/RemoteCommandProtocol.cs",
        "span": "1-52"
      },
      {
        "path": "app/native/CodexLocalRetrieval.Native/MainPage.Remote.cs",
        "span": "340-480"
      },
      {
        "path": "app/native/CodexLocalRetrieval.Native/MainPage.Remote.cs",
        "span": "1060-1100"
      },
      {
        "path": "app/native/CodexLocalRetrieval.Core/Remote/RemoteBridge.cs",
        "span": "1-510"
      },
      {
        "path": "app/native/CodexLocalRetrieval.Native.Tests/RemoteCommandProtocolTests.cs",
        "span": "1-38"
      },
      {
        "path": "app/native/CodexLocalRetrieval.Native.Tests/RemoteApiTests.cs",
        "span": "1-298"
      }
    ]
  },
  {
    "title": "Validate ws control frames + fetchfile-contract regression",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "index.html's onControl (relay/public/index.html:1254-1261) accepts any JSON after the 'd' prefix and feeds m.cols/m.rows straight into term.resize while m.clients is used without an array check ((sizeClients||[]).filter at :1266 throws on truthy non-arrays). Add shape/range validation before mutating terminal state: cols/rows must be finite integers within sane bounds (e.g. 2..1000 cols, 2..500 rows) else the frame is ignored; clients must pass Array.isArray else []; mode/pinLabel coerced to expected primitive types. Extend the static client suite (new file tests/frame-guard.test.js — vm-extract the onControl function and drive it with malformed frames): non-numeric/huge/NaN dims never reach term.resize; non-array clients never throws. Include the fetchfile regression lock (fixed client-side by the merge): static assertions that the fetchfile postIntent payload always carries an insert mode (:2309) and that pollUploadCmd's detail is used only as opaque status, never inserted into the terminal or used as a path. SEAM: do not touch the input queue path — the native prong owns input continuity in the same region.\n\nTRUST AMENDMENT: keep this as parser and shape hardening. It must not claim to defend against a compromised relay; authority-bearing frames are accepted only after endpoint principal verification in r.8.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/frame-guard.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "1250-1270"
      },
      {
        "path": "relay/public/index.html",
        "span": "2290-2320"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "Escape the status/flash HTML sink",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "setStatus (relay/public/index.html:1559) writes its text argument via innerHTML, and flash (:1999) forwards arbitrary strings into it; unescaped taint reaches the sink via collection names (:1951-1968), API/bridge error details (:1968, :1987-1993), and filenames (:2307). Split the API: setStatus becomes text-only (textContent), and the small number of trusted-markup call sites (e.g. flash's live-status restore template, which already escapes via esc()) move to an explicit setStatusHtml used ONLY with literal templates whose dynamic parts are esc()-wrapped. Audit every setStatus/flash caller so no user/bridge-derived string reaches innerHTML unescaped. New relay/tests/status-sink.test.js (static + vm-extract): flash('<img src=x onerror=1>') produces no element markup in the status node; every setStatusHtml call site in the source matches an allowlist regex proving esc()-wrapping of interpolated values. Scope is the status/flash sink only; other innerHTML sites (:1771, :1814, :1853, :2086, :2416) already esc() their dynamic parts — verify in passing.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/status-sink.test.js",
    "reads": [
      {
        "path": "relay/public/index.html",
        "span": "1550-1570"
      },
      {
        "path": "relay/public/index.html",
        "span": "1940-2010"
      },
      {
        "path": "relay/public/index.html",
        "span": "2300-2310"
      },
      {
        "path": "relay/tests/client-layout.test.js",
        "span": "1-53"
      }
    ]
  },
  {
    "title": "muxd custody TTL: dormant-record GC + stale-emission filter",
    "kind": "build",
    "tier": "default",
    "deps": [
      "1"
    ],
    "goal": "CENTERPIECE. Session manifest records in muxd get an explicit custody lifecycle: add lastAliveUtc + custodyExpiresUtc to session records (schema-additive; valid_session_records at muxd.py:2010 tolerates unknown keys), refresh on the status pump for alive sessions, and GC dormant/failed records whose custody expired (default TTL 24h, env MUX_CUSTODY_TTL_S) at boot (:3341-3444 region, AFTER the existing user_killed removal at :3395-3413 — that partial fix already landed, do not re-implement) and periodically. live_tabs_snapshot (:2109-2128) and sess_list (:2301-2302) stop emitting custody-expired records entirely (pinned contract: filter, not label — the relay keeps showing unexpired dormant rows for relaunch exactly as today). Never GC a record holding a live launch claim or identityPending within grace. Extend muxd/tests/test_muxd_state.py: (a) boot over manifest {alive, fresh-dormant, expired-dormant, user_killed} retains exactly alive+fresh-dormant; (b) live_tabs_snapshot and sess_list contain no expired entries; (c) TTL refresh on simulated status pump; (d) claim-holder and identityPending records survive. Append the custody fields to docs/protocol-fields.md. muxd deploys freely.",
    "verifier": "cd muxd && timeout 240 python -m unittest tests.test_muxd_state -v",
    "reads": [
      {
        "path": "muxd/muxd.py",
        "span": "1983-2060"
      },
      {
        "path": "muxd/muxd.py",
        "span": "2109-2135"
      },
      {
        "path": "muxd/muxd.py",
        "span": "2266-2302"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3341-3470"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3700-3740"
      },
      {
        "path": "muxd/tests/test_muxd_state.py",
        "span": "1-300"
      },
      {
        "path": "docs/protocol-fields.md",
        "estTokens": 6000
      }
    ]
  },
  {
    "title": "Process-truth agent state, muxd producer",
    "kind": "build",
    "tier": "default",
    "deps": [
      "1"
    ],
    "goal": "muxd reports whether the agent process is actually alive: add agentTruth {procAlive, cpuActiveRecent, exe, checkedUtc} plus agentStateSource ('process'|'heuristic') to session_payload (muxd.py:2266-2288, which already carries childPid), fed by a bounded probe over the session's childPid descendants (reuse _pid_descends_from machinery at :1042). Executor discipline is mandatory: probes on a small dedicated executor, results cached >=10s, per-probe timeout <=3s, never under the state lock; on stale/failed probe, agentStateSource degrades to 'heuristic' and the existing session_agent_status (:2231; shell-prompt death regex :2225-2228) output stands. Announce cap 'agentTruth' in CAPS (muxd.py:907) and append the fields to docs/protocol-fields.md. Do NOT bump PROTOCOL (relay requires ==4); additive field + additive cap only. New muxd/tests/test_agent_truth.py: real dummy child (python -c 'import time; time.sleep(60)') under a fake session -> procAlive true; kill it -> flips false within one probe interval; fault-injected hung probe leaves a status call responsive <1s and source 'heuristic'.",
    "verifier": "cd muxd && timeout 180 python -m unittest tests.test_agent_truth -v",
    "reads": [
      {
        "path": "muxd/muxd.py",
        "span": "1035-1060"
      },
      {
        "path": "muxd/muxd.py",
        "span": "2220-2300"
      },
      {
        "path": "muxd/muxd.py",
        "span": "900-910"
      },
      {
        "path": "muxd/tests/test_muxd_state.py",
        "span": "1-300"
      },
      {
        "path": "docs/protocol-fields.md",
        "estTokens": 6000
      }
    ]
  },
  {
    "title": "muxd per-session output queues + explicit resync frame",
    "kind": "build",
    "tier": "default",
    "deps": [
      "1"
    ],
    "goal": "One flooding session can no longer starve or gap other sessions' relay output: replace the single shared RelayOutQueue (muxd.py:1211-1223, maxsize 64, silent tail-drop with a dropped counter logged at :3727-3732 — the base plan's mid-stream trim no longer exists; the current defect is silent tail-drop on the shared queue) with per-session byte-bounded queues (~1MiB each) drained round-robin into the relay ws; on per-session overflow, drop only that session's backlog to a boundary and enqueue a {t:'resync', s} frame. Add the <=15-line relay-side handler for t:'resync' that triggers the existing CLEAR+scrollback replay (requestSessionScrollback, server.js:2139) for that session's viewers, and register frame+cap ('resync') in docs/protocol-fields.md and muxd CAPS. This single leaf owns the frame end-to-end (both sides). New muxd/tests/test_output_fairness.py: session A floods 10MiB while B emits 100 numbered lines -> all 100 B frames egress in order; force A over budget -> exactly one resync for A, none for B; round-robin interleaving asserted. New relay/tests/resync-handler.test.js: fake host sends resync -> relay issues an sb request / CLEAR+replay to that session's viewers only.",
    "verifier": "cd muxd && timeout 240 python -m unittest tests.test_output_fairness -v && cd ../relay && node --test --test-force-exit --test-timeout=20000 tests/resync-handler.test.js",
    "reads": [
      {
        "path": "muxd/muxd.py",
        "span": "1205-1230"
      },
      {
        "path": "muxd/muxd.py",
        "span": "3700-3740"
      },
      {
        "path": "relay/server.js",
        "span": "2130-2160"
      },
      {
        "path": "docs/protocol-fields.md",
        "estTokens": 6000
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      }
    ]
  },
  {
    "title": "Process-truth agent state, relay consumer",
    "kind": "build",
    "tier": "default",
    "deps": [
      "14"
    ],
    "goal": "Relay state dots and autoheal prefer host agentTruth when present: precedence = fresh host agentTruth > host heuristic > relay pane-regex (paneAgentState server.js:339, hostAgentStatus :385, attentionStatusForHosted :399); autoheal death-recovery for hosted sessions may fire only on procAlive===false or explicit host dormant, never on regex alone; absent field = exactly today's behavior (old muxd stays compatible via hostSupportsCap('agentTruth'), server.js:271). New relay/tests/agent-truth.test.js (relay.test.js MUX_STATE_DIR harness style): fake host ws pushes (a) procAlive:false + healthy-looking footer -> red/heal-eligible; (b) procAlive:true + footer matching death regex -> not red, no heal; (c) no agentTruth field -> legacy behavior preserved.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/agent-truth.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "330-470"
      },
      {
        "path": "relay/server.js",
        "span": "1770-1860"
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      },
      {
        "path": "docs/protocol-fields.md",
        "estTokens": 6000
      }
    ]
  },
  {
    "title": "Ops alerts: push on degraded-health edge transitions",
    "kind": "build",
    "tier": "default",
    "deps": [],
    "goal": "The relay currently has zero alerting (no ntfy/webhook code). Add a small relay/health-alerts.js module watching the existing degraded computation (server.js:1170-1190 region), persistenceBlocked, host-link-down duration, and gave-up heals; fire on EDGE transitions only (healthy->degraded sustained >2min; host link down >5min; heal gave-up; persistenceBlocked), one push per condition per 30min dedupe window, plus a recovery push. CROSS-PRONG CONTRACT (pinned, no hard dep): transport is relay/notify.js OWNED BY THE VALUE PRONG (batch r.2 child 2) with API createNotifier({url, fetchImpl}) -> notifier.push({title, body, tags, priority, click}) returning {ok,...} and never throwing — inject the notifier (constructor/function param) so tests use a mock; do NOT rebuild transport; if notify.js is not yet merged when this leaf starts, build against that pinned API on this branch. Secrets stay in /etc/multiplex-app.env (document, never commit). NOTE: /api/health.gaveUp is a hardcoded 0 stub today (server.js:1170) and muxd does not report heal-give-up over the host link — alert on the signals that exist (degraded, persistenceBlocked, host-link-down) and leave a documented TODO hook for gave-up pending a muxd signal. New relay/tests/alerts.test.js: drive synthetic health states with a mock transport; assert fire-on-edge, dedupe window, recovery notice, and NO fire on a 30s transient blip.",
    "verifier": "cd relay && node --test --test-force-exit --test-timeout=20000 tests/alerts.test.js",
    "reads": [
      {
        "path": "relay/server.js",
        "span": "1130-1195"
      },
      {
        "path": "relay/notify.js",
        "estTokens": 2500
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      }
    ]
  },
  {
    "title": "relay npm test = real bounded deploy gate + engines pin",
    "kind": "build",
    "tier": "default",
    "deps": [
      "3",
      "4",
      "5",
      "6",
      "16",
      "17"
    ],
    "goal": "Make relay/package.json's scripts.test a real bounded gate: node --test --test-force-exit with a file-level timeout >=180s over tests/*.test.js (excluding tests/browser/), replacing the stock error stub (:7) — the suite legitimately takes 72-78s for relay.test.js alone, so 20s file budgets are wrong; add engines {node: '>=22'} and fix main. Fix or document the non-terminating teardown in tests/relay.test.js (orphaned child server.js processes) — if a clean fix is out of reach in one sitting, keep --test-force-exit and write the why into package.json comments/README section. Wire scripts/deploy-relay.sh to run npm test before restart and abort on failure. Runs LAST among relay leaves (the deps encode that). Does NOT touch app/tools/run_gates.ps1 — the value prong owns widening the run_gates relay-tests gate.\n\nTRUST AMENDMENT: this is the reliability baseline gate (renumbered from old child 19 after removal of the deep security planner). Trust tests land later in root trust leaves r.5 through r.16 and are rerun by the trust smoke, the widened value gate, and the final campaign judge.",
    "verifier": "cd relay && timeout 240 npm test && powershell -NoProfile -Command \"$p=Get-CimInstance Win32_Process|Where-Object{$_.Name -eq 'node.exe' -and $_.CommandLine -match 'relay[\\\\/]server\\.js'};if($p){$p|Select-Object ProcessId,CommandLine|Out-String|Write-Error;exit 1}\"",
    "reads": [
      {
        "path": "relay/package.json",
        "span": "1-17"
      },
      {
        "path": "relay/tests/relay.test.js",
        "span": "1-130"
      },
      {
        "path": "scripts/deploy-relay.sh",
        "span": "1-8"
      }
    ]
  }
]
